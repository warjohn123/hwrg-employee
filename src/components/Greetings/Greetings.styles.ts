import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 20,
    borderWidth: 1,
    borderColor: "#aaa",
    borderRadius: 8,
    display: "flex",
  },
  header: {
    fontSize: 16,
    fontWeight: "700",
    marginBottom: 20,
    color: "#111827",
  },
});
