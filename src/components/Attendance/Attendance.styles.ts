import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  clockInButton: {
    backgroundColor: "#22c55e",
    borderRadius: 10,
    paddingVertical: 14,
    alignItems: "center",
    marginBottom: 24,
  },
  clockOutButton: {
    backgroundColor: "red",
    borderRadius: 10,
    paddingVertical: 14,
    alignItems: "center",
    marginBottom: 24,
  },
  clockInText: {
    color: "#fff",
    fontSize: 16,
    fontWeight: "600",
  },
});
